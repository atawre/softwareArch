Usage - Only single digit number are allowed.
Expression length - As big as java string.
Expression input format - Infix, fully paranthesized.
Expression tree can be seen on console.
Traversal and tree value in disabled text box on GUI.
Few more patterns are present in code e.g. facade, builder for which some code rearrangement is needed.
In addition to MVC, I am adding Dockerfile to for running the jar file i.e. exprCalc.jar

$ java -jar exprCalc.jar

